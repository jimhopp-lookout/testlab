# -*- mode: ruby -*-
# vi: set ft=ruby :
#
# Auto-generated by cucumber-chef v#{Cucumber::Chef::VERSION} on #{Time.now.utc} -- DO NOT EDIT!

# Vagrant v1 Configuration
###########################
Vagrant::Config.run do |config|
  config.vm.define <%= @vagrant[:id].inspect -%> do |testlab|
    testlab.vm.box = "precise64"
    testlab.vm.box_url = "http://files.vagrantup.com/precise64.box"
    testlab.vm.network :hostonly, <%= @vagrant[:ip].inspect %>

    testlab.vm.customize ["modifyvm", :id, "--cpus", <%= @vagrant[:cpus].inspect %>]
    testlab.vm.customize ["modifyvm", :id, "--memory", <%= @vagrant[:memory].inspect %>]
  end
end

# Vagrant v2 Configuration
###########################
Vagrant.configure("2") do |config|
  config.vm.define <%= @vagrant[:id].inspect -%> do |testlab|
    testlab.vm.box = "precise64"
    testlab.vm.box_url = "http://files.vagrantup.com/precise64.box"
    testlab.vm.network :private_network, :ip => <%= @vagrant[:ip].inspect %>

    testlab.vm.provider :virtualbox do |vb|
      vb.customize ["modifyvm", :id, "--cpus", <%= @vagrant[:cpus].inspect %>]
      vb.customize ["modifyvm", :id, "--memory", <%= @vagrant[:memory].inspect %>]
    end
  end
end
