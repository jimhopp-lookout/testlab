$TTL 1d
@ IN  SOA localhost. root.localhost. (
   <%= Time.now.utc.to_i %>   ; Serial
           1w   ; Refresh
           1d   ; Retry
           4w   ; Expire
           1d ) ; Negative Cache TTL
;
@ IN  NS  localhost.
<% @records.each do |record| -%>
<%= record %>
<% end -%>
